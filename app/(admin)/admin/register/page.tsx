"use client";
import { FormEvent, useState } from "react"; import Link from "next/link";
export default function RegisterPage(){const[message,setMessage]=useState('');const[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMessage('');setError('');const fd=new FormData(e.currentTarget);const res=await fetch('/api/auth/register',{method:'POST',body:JSON.stringify({name:fd.get('name'),email:fd.get('email'),password:fd.get('password')}) ,headers:{'content-type':'application/json'}});const data=await res.json();if(!res.ok)setError(data.error||'Unable to register.');else{setMessage(data.message);(e.currentTarget as HTMLFormElement).reset()}}return <main className="min-h-screen bg-[#07070B] text-ink-0 flex items-center justify-center px-4"><form onSubmit={submit} className="w-full max-w-md bg-[#0B0A10] border border-ink-800 rounded-2xl p-8"><p className="text-xs tracking-[0.25em] uppercase text-ink-500 mb-3">VIBRANT 2K26</p><h1 className="text-3xl font-display font-bold">Admin Registration</h1><p className="text-ink-400 text-sm mt-2 mb-8">New registrations start as PENDING and cannot self-assign SUPER_ADMIN.</p>{message&&<div className="mb-5 rounded-lg border border-green-500/20 bg-green-500/10 p-3 text-sm text-green-300">{message}</div>}{error&&<div className="mb-5 rounded-lg border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-300">{error}</div>}<div className="space-y-4"><input name="name" required minLength={2} placeholder="Full name" className="w-full bg-[#07070B] border border-ink-800 rounded-lg px-4 py-3"/><input name="email" type="email" required placeholder="Email" className="w-full bg-[#07070B] border border-ink-800 rounded-lg px-4 py-3"/><input name="password" type="password" required minLength={12} placeholder="Password (12+ characters)" className="w-full bg-[#07070B] border border-ink-800 rounded-lg px-4 py-3"/></div><button className="w-full mt-6 bg-vibeesta-600 hover:bg-vibeesta-500 text-white rounded-lg px-4 py-3 font-semibold">Submit Registration</button><Link href="/admin/login" className="block text-center mt-5 text-sm text-ink-500 hover:text-ink-300">Back to login</Link></form></main>}
