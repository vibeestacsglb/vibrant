"use client";
import { FormEvent,useState } from 'react'; import Link from 'next/link';
export default function AdminLoginPage(){const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const fd=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:fd.get('email'),password:fd.get('password')})});const data=await res.json().catch(()=>({}));if(!res.ok){setError(data.error||'Invalid credentials.');setBusy(false);return}window.location.href='/admin'}return <main className="min-h-screen bg-[#07070B] text-ink-0 flex items-center justify-center px-4"><form onSubmit={submit} className="w-full max-w-md bg-[#0B0A10] border border-ink-800 rounded-2xl p-8"><p className="text-xs tracking-[0.25em] uppercase text-ink-500 mb-3">VIBRANT 2K26</p><h1 className="text-3xl font-display font-bold">Admin Login</h1><p className="text-ink-400 text-sm mt-2 mb-8">Authorized administrators only.</p>{error&&<div className="mb-5 rounded-lg border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-300">{error}</div>}<div className="space-y-4"><label className="block text-sm text-ink-300">Email<input name="email" type="email" required autoComplete="username" className="mt-2 w-full bg-[#07070B] border border-ink-800 rounded-lg px-4 py-3"/></label><label className="block text-sm text-ink-300">Password<input name="password" type="password" required autoComplete="current-password" className="mt-2 w-full bg-[#07070B] border border-ink-800 rounded-lg px-4 py-3"/></label></div><button disabled={busy} className="w-full mt-6 bg-vibeesta-600 hover:bg-vibeesta-500 disabled:opacity-60 text-white rounded-lg px-4 py-3 font-semibold">{busy?'Signing in…':'Sign in'}</button><Link href="/admin/register" className="block text-center mt-5 text-sm text-ink-500 hover:text-ink-300">Need an account? Register for approval</Link></form></main>}
